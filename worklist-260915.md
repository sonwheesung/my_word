# 화요일(2026-09-15) 작업 목록 — 2026-09-14 세션 인계

> 양식 정본: `C:/project/common/WORKLIST_HANDOVER.md`.
> ⚠ 09-15 는 화요일이다(09-14 가 월요일). 9-11 에 "월요일(09-15)" 로 잘못 세었던 것을 9-14 에 고쳤다.

---

## 0. 끝남: 결과 화면 수정 넷을 OTA 로 냈다 (2026-09-18 · 그룹 `428aa669` · 에뮬레이터 수신 확인)

- 스크롤 · 다시 풀기 · 뒤로가기 확인 · 영어 라벨. 발행 기록은 CHANGELOG `[1.7.0 OTA]`
- 서버 통계에서 버전 수가 바뀌어도 사용자 증감이 아니다(OTA 는 앱 버전 번호를 바꾸지 않는다)
- 남긴 둘: 플래시카드 순서가 퀴즈에서 유지되지 않음 · `틀린 N개` 의 N 이 오답 건수

### 고치기 전 기록 (원인 설명으로 남긴다)

증거 화면: `D:\builds\my_word\evidence\2026-09-18\` (1~4번)

### ① 🔴 틀린 단어가 많으면 결과 화면이 스크롤되지 않는다 (1.3.2 부터)
**현상**: 8개 · 10개 틀린 판에서 결과 화면이 **위(점수)와 아래(홈으로 · 다시 풀기 버튼)가 잘린 채 멈춘다.** 위아래 어느 쪽으로도 안 움직인다.
빠져나갈 길은 기기 뒤로가기뿐이다. 같은 에뮬레이터에서 퀴즈 설정 화면은 정상으로 스크롤됐다(입력 문제가 아니다).
**원인**: `QuizResultScreen` 의 `styles.content` 가 `flex: 1` + `justifyContent: 'center'` 다. 스크롤 안의 내용이 화면 높이에 묶여 넘친 만큼 위아래로 잘린다.
2026-08-21 `55bf65c` 가 틀린 단어 목록을 모달에서 화면 안으로 옮기면서 생겼다(그 전에는 모달 안이 스크롤이었다).
**곁가지**: 영어에서 `Correct answer` · `Your answer` 라벨이 좁은 칸에서 `Corre / ct ans / wer` 로 글자 단위로 끊긴다.

### ② 다시 풀기가 홈으로 떨어진다 (1.6.0 부터)
**현상**: 홈 복습 배너나 플래시카드에서 시작한 퀴즈를 끝내고 결과 화면에서 `다시 풀기`(오답이 있으면 `전체 다시 풀기`)를 누르면 **홈이 뜬다.**
그 홈에서 기기 뒤로가기를 누르면 종료 확인 대신 **퀴즈 설정**이 열린다(화면 상태가 퀴즈로 남아 있다는 증거).
**원인**: `App.tsx` 의 `onRetry` 가 단어 목록(`retryWordIds`)을 비우는데, 이 두 길은 카테고리(`quizCategoryId`)가 null 이다.
그래서 퀴즈 렌더 조건(`quizCategoryId !== null || retryWordIds`)이 거짓이 되고 마지막 기본 분기인 Home 이 그려진다.

같은 대조에서 나온 것(묶어서 한 번에 고칠지 사장님 판단):
| 무엇 | 자리 |
|---|---|
| 하드웨어 뒤로가기가 확인창 없이 퀴즈를 나간다. 복습 배너에서 왔으면 홈이 아니라 퀴즈 설정으로 간다 | `App.tsx` 뒤로가기 `case 'quiz'` |
| 플래시카드에서 넘긴 순서가 퀴즈에서 유지되지 않는다(저장소 순서) | `QuizScreen` 의 `words.filter(... retryWordIds.includes ...)` |
| `틀린 N개 다시 풀기` 의 N 은 오답 건수, 실제 문제는 중복을 걷은 단어 수 | `QuizResultScreen` · `App.tsx` `wrongWordIds` |
| (흠) 단어장의 `onAddWord` prop 을 쓰는 곳이 없다 · `srsService.invalidate` 를 부르는 곳이 없다 | |

- ~~코드 수정은 계획 → 승인 뒤~~ → 2026-09-18 사장님 지시("수정하고 에뮬레이터로 확인도 해")로 넷을 고쳤다. 위 첫 줄 참조.
  고친 뒤 증거 화면 5~8번. 표의 넷째 줄(흠)과 순서 · N 은 남겼다

### 끝남: 스토어 스크린샷 검토 (2026-09-18 확인)
- 공개 스토어 페이지에서 한국어 · 영어 모두 **새 네 장이 맨 앞에** 보인다(검토 통과 · 게시됨)
- 한국어 페이지 뒤쪽 열 장은 비율이 10:16 이라 기존 태블릿 스크린샷으로 보인다(휴대폰 새 장은 9:16). 영어 페이지는 네 장뿐

### 이어서 볼 것
1. `모니터링 및 개선 → 권장 조치` 에서 R8 항목이 **2줄에서 1줄**(AGP 9.0 만)로 줄었는지. 1.7.0 게시 뒤 반영까지 시간이 걸릴 수 있다
2. 공용 `PLAY_CONSOLE_STATUS.md` My Word 행을 22 (1.7.0) 으로 고친다.
   🔴 그 파일에 **다른 세션의 커밋 안 된 수정**이 있다. 파일째 `git add` 하지 않는다. 내 행만 담거나 커밋은 그 세션에 맡긴다
3. 서버 통계를 읽을 때: 14:13 OTA 로 **1.6.0 설치자도 1.7.0 JS 를 받아 1.7.0 으로 잡힌다.** 버전별 수가 갑자기 바뀌어도 사용자가 늘거나 준 것이 아니다
4. 공지 노출이 **9/21 09:00 KST** 에 끝난다. 그 뒤 새 공지를 올릴지는 사장님 판단
5. **app-ads.txt 경고가 사라졌나**(2026-09-18 콘솔 `주의` 로 뜬 것). 9/18 에 My Word · 조각 스토어 연락처에 웹사이트
   `https://vivace-games.com` 을 넣었다. AdMob 이 다시 읽기까지 하루쯤 걸린다. AdMob `앱 → app-ads.txt` 에서 두 앱이
   "찾음/인증됨" 인지, Play 콘솔의 경고가 없어졌는지 본다. 공개 스토어 페이지에 `웹사이트` 가 보이는지도 본다(9/18 이미 보임)
6. **AdMob 앱 인증이 넘어갔나.** 9/18 사장님 승인으로 다섯 앱 모두 Play 앱을 연결했다(스토어 미연결로 `광고 게재가 제한됨` 이었다).
   배구명가는 그 자리에서 인증을 통과해 `검토 중` 이다. My Word · LinkMemo · Idea Repository · 조각 넷은
   *"세부정보가 일치하지 않는 것 같습니다"* 로 떨어졌다. 파일은 맞고, AdMob 이 `vivace-games.com` 을 아직 한 번도 크롤링하지 않았다.
   → AdMob `앱 → app-ads.txt` 에 `vivace-games.com` 줄이 생겼나, 넷이 `검토 중` 으로 넘어갔나 본다.
   안 넘어갔으면 각 앱 설정에서 앱 인증을 다시 누른다. 정본: 공용 `APP_ADS_TXT.md` §1.2

---

## 1. 백로그

### 유저 피해가 실재
**고쳐서 OTA 로 냈다**(2026-09-18). 위 §0. 9-10 에 적은 "연속 학습일이 실제보다 길다" 는 9-14 에 **결함이 아닐 가능성이 커졌다.**
단어 등록일(9/1~9/9)과 퀴즈일(9/10~9/13)을 심자 `13d` 가 나왔고 계산과 맞았다. 등록일도 활동으로 센다.
확정하려면 9-10 의 시드를 다시 대 본다.

### 보안·수익
없음.

### 검증 도구 · 못 본 화면
| 항목 | 왜 |
|---|---|
| 발음 소리를 한 번도 못 들었다 | 에뮬레이터를 `-no-audio` 로 띄운다. 실기기에서 들어 봐야 안다 |
| 만기순 정렬을 화면으로 못 봤다 | 9-14 에 설정 화면의 `10 due` 까지만 봤고 카드는 `등록순` 으로 넘겼다 |
| 시트 두 개 | 알림 권유 · 복원 확인. 여백 공통화 뒤 눈으로 안 봤다 |

### 표면 흠
없음.

### 문서가 코드보다 낡은 것
~~architecture · screens · data-model · test-cases · README~~ → **2026-09-18 문서 감사로 고쳤다**(아래 §2). 재발은 `npm run check:docs` 가 잡는다.

⏸ **`CLAUDE.md` 에서 찾은 것. 고치기 전에 사장님께 올린다**(프로젝트 규칙):
| 절 | 적힌 것 | 실제 |
|---|---|---|
| R8 · 1.4.0 게시 절 | *"🔴 심사 통과 후 확인할 것: 난독화 경고가 사라지는가"* | 같은 절 위에 "경고가 사라졌다" 가 이미 있다 |
| 최적화된 리소스 축소 절 | *"🔴 검증이 남았다. 다음 릴리스를 구울 때 반드시 한다"* | 9-14 1.7.0 에서 검증했다(바로 위 줄만 취소선) |
| R8 · 1.4.0 준비 절 | *"🔴 `mapping.txt` 를 함께 업로드한다"* | 바로 아래 절이 "AAB 안에 들어 있다 · 별도 업로드 절차를 만들지 말 것" |
| 문서에 없는 것 | `npm run check:docs` · `doc-consistency` 스킬 | CLAUDE.md 가 가리키지 않으면 세션이 모른다 |
| ⏸ 닫을 근거가 생긴 것 | OTA 절 *"npx expo export 로 새 번들을 구워 보는 것은 못 했다"* | CHANGELOG 9-14 줄에 "발행 전 번들 주소 검사 통과"가 있었다(감사 때 못 찾았다). 9-18 에도 굽고 검사했다 |

---

## 2. 끝낸 것

### 9월 18일 (목)
- Play 콘솔 `주의` "app-ads.txt 추가 또는 업데이트" 원인: **My Word 스토어 연락처의 웹사이트 칸이 비어 있었다.**
  `vivace-games.com/app-ads.txt` 는 처음부터 맞게 있었다. 같은 광고 계정 다섯 앱을 대조해 조각도 비어 있는 것을 찾았다
- 사장님 지시로 My Word · 조각 웹사이트에 `https://vivace-games.com` 을 넣고 게시했다(콘솔 "변경사항 게시됨" 확인)
- 공용 `APP_ADS_TXT.md` 신설(다섯 앱 표 · 새 광고 앱 절차 · 이메일을 웹사이트로 오판하는 함정)
- `/check` 스킬 등록 `2e6b78a`. 사장님이 시트에 `my_word` 탭을 만들었다. 머리글이 양식과 같고, 첫 확인은 0건이다(기준 저장)
- AdMob 다섯 앱 모두 Play 스토어 연결(사장님 승인). 배구명가만 인증 통과 · 나머지 넷은 크롤링 대기(위 §0 이어서 6)
- 스토어 스크린샷 검토 통과 확인(공개 페이지 한국어 · 영어)
- **문서 감사**: 설계 문서 넷(`architecture` · `screens` · `data-model` · `services-api`)을 코드에서 다시 뽑아 고쳤다.
  README 개수 · OTA 명령 · 런타임 버전, `test-cases` 현황 절과 RT-08~13, `I18N_PLAN` 상태, CHANGELOG 1.5.0 제목
- 가드 `npm run check:docs` 신설(화면 · 서비스 · 컴포넌트 · 저장 키 이름 집합 · 회귀 주입으로 빨개지는 것 확인)
- 스킬: My Word `doc-consistency` 신설 · `reload-docs` 에 `check:docs` 추가 · 공용 `doc-consistency` 를 형제 판 셋과 합쳐 올림
- 에뮬레이터에서 결과 화면 결함 확인 → 넷을 고침(스크롤 · 다시 풀기 · 뒤로가기 · 영어 라벨). 릴리스 APK 로 통과 → **OTA 발행 · 수신 확인**(그룹 `428aa669`)

### 9월 14일 (월) 오후
- 1.7.0 게시 완료(사장님 확인) · CHANGELOG 출시 줄 `db27f4b`
- 공지를 앱 언어로 고른다: 공통 서버 SDK 2026-09-14 재복사 · 공지 화면 · 시험 4개(288/288) `db27f4b`
- 그 변경을 **OTA 로 냈다**(14:13 · production · runtime 1.6.0 · 그룹 `e2edd66c`) `e55c617`. 에뮬레이터 1.7.0 스토어 빌드에서 수신 확인
- **1.7.0 공지 발행**(한국어 · 영어 · 상단 고정 · 9/14 09:00 ~ 9/21 09:00 KST) `6ed5dd2`. 영어 앱 화면에 영어로 뜨는 것 확인 `3cc0b76`
- **스토어 스크린샷 교체**: ChatGPT 배경 한 장 + 실제 화면 합성으로 여덟 장(한국어 · 영어 × 4). 콘솔 교체 · 저장 `e18b5f7`, 검토 제출은 사장님
- 공통 서버 세션에 OTA · 공지 · 기기 확인 결과를 알렸다(콘솔 "최신 버전 안내" 가 1.3.3 인 것도 함께)
- 공용 `STORE_SCREENSHOTS.md` 신설 · 보강, 도구 `tools/storeshots/`, 예시 이미지 (common `90793af` · `2684f9e`)

### 9월 14일 (월) 오전
- 1.7.0 검토 제출 `b545d2a` (10:37 · 사장님이 직접 제출 · 지원 기기 감소 0대 · 설치 15.7MB)
- `CLAUDE.md` 에 1.7.0 확인 절 `2c34ae7` (같은 날 그 절에 박힌 제어문자 1개를 따로 정정했다)
- 출시 노트 한 줄이 동작과 달랐던 것 `4fb03af` (오른쪽으로 밀면 이전 카드다)
- 릴리스 빌드 확인 전 항목 통과 `6356d15` (알림 아이콘 실물 · 채점 안 함 md5 전후 동일 · 오프라인 오류 0)
- 버전 1.7.0 / vc22 · 고지 재생성 · CHANGELOG `6351965`
- 출시 노트 세 언어 `76173f3`
- 공용 `PRE_LAUNCH_CHECK.md` 정정: 새 버전 2단계 저장은 전송이 아니었다 (common `f05e766`)
- 빌드 찌꺼기 2.3GB 정리 · AAB 는 `D:/builds/my_word/myword-vc22.aab` 에 보관

### 9월 10~11일
- 스킬 둘이 틀린 규칙을 들고 있던 것 `1ba457f` (창 모드는 `-gpu host` 면 뜬다 · 고지 판정은 가드로)
- `CLAUDE.md` 에 플래시카드 절 `a1bf7ba` (채점하지 않는 것이 결정이지 미완성이 아니다)
- 게시 중인 오픈소스 고지가 두 릴리스 낡아 있던 것 `0be84b2`
- 시트 좌우 여백의 주인을 공용 부품으로 옮김 + 뜻 목록 한 곳으로 `333578e`
- 카드를 넘기면 카드가 사라지던 것 `097a246` (타입 검사와 시험 284개를 전부 통과한 채 있던 결함)
- 플래시카드 `aa391eb`
- 최적화된 리소스 축소를 켬 `6367fd6` (9-14 릴리스 빌드에서 검증)

---

## 3. 대기 중

| 무엇 | 누구를 기다리나 |
|---|---|
| 공통 서버 콘솔 "최신 버전(안내)" 값이 1.3.3 | 공통 서버 세션(알림 보냄). 앱은 1.7.0. 서버 설정이라 My Word 가 고치지 않는다 |
