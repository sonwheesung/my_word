# 월요일(2026-09-15) 작업 목록 — 2026-09-11 세션 인계

> 양식 정본: `C:\project\common\WORKLIST_HANDOVER.md` (2026-09-10 신설).
> 공백이 나흘이다. 다시 열 때 §5.2 의 `2~7일` 줄을 먼저 본다.

---

## 0. 바로 시작할 것 — 1.7.0 을 스토어에 올린다 (사용자 승인 완료: *"월요일에 업데이트 진행할거니깐"*)

- **정본**: `CLAUDE.md` 의 네이티브 수동 동기화 표 + R8 절 · 절차는 `.claude/skills/emulator-test`
- **재현·검증**: 아래 두 줄을 **손대기 전에** 먼저 돌린다. 지금은 둘 다 초록이어야 한다.
  ```bash
  npx tsc --noEmit && node scripts/check-tests.mjs   # 284/284 · 스위트 14/15(알려진 고장 1)
  npm run check:licenses                             # ALL PASS · 218개
  ```
- 핵심 네 걸음:
  1. **버전을 손으로 네 곳에 올린다.** `app.json`(version · versionCode) · `android/app/build.gradle`(versionName · versionCode). 🔴 `runtimeVersion` 과 `strings.xml` 의 `expo_runtime_version` 은 **1.6.0 그대로 둔다.** 이번에 네이티브 모듈이 안 늘었기 때문이다. 올리면 기존 사용자가 무선 업데이트를 못 받는다.
  2. **오픈소스 고지를 다시 굽는다**(`node scripts/generate-oss-licenses.js`). 고지에 앱 버전이 박혀 있어 **버전을 올리면 그 자리가 낡는다**(오늘 실제로 두 릴리스 낡아 있었다 · `0be84b2`).
  3. **CHANGELOG 에 1.7.0 을 쓴다.** 내용은 플래시카드다. 아직 한 줄도 없다.
  4. 🔴 **릴리스 빌드로 눈으로 본다.** 아래 §0.1.
- 주의: 🚫 `expo prebuild` 를 돌리지 않는다. 서명 설정이 `android/` 에 손으로 들어가 있고 git 에 없다. 🚫 `./gradlew clean` 도 쓰지 않는다(이 구성에서 죽는다).

### 0.1 🔴 릴리스 빌드에서 반드시 눈으로 볼 것

지난주에 확인한 것은 **디버그 빌드**다. 코드 축소는 디버그에 안 걸리므로 그 확인은 이번 건에 쓸 수 없다.
이번 릴리스에는 **지난주에 새로 켠 설정 한 줄**(`android.r8.optimizedShrinking=true`)이 처음 실린다.
이 설정은 우리 빌드 도구에서 아직 실험 등급이고, **화면에서 이름으로만 불리는 그림을 더 공격적으로 지운다.**

| 볼 것 | 왜 |
|---|---|
| 🔴 **알림 아이콘이 흰 네모가 되지 않는가** | 알림 아이콘은 설정 파일에서 이름으로만 불린다. 지워지면 흰 네모가 된다. 🔴 파일 이름으로 찾으면 판정이 안 된다(이름이 난독화된다). **알림을 실제로 띄워서 본다** |
| 🔴 **무선 업데이트 기능이 살아 있는가** | 이게 죽으면 **다음 무선 업데이트로 그걸 고칠 수단이 없다.** 스스로를 못 고치는 유일한 자리다 |
| 카드 뒤집기 · 넘기기 | 이번에 새로 넣은 화면이다 |
| 비행기 모드 첫 실행 | 오류 문구가 **0건**이어야 한다 |

되돌리는 법은 `android/gradle.properties` 에서 그 한 줄을 지우는 것이다.

---

## 1. 백로그

### 유저 피해가 실재
| 순위 | 항목 | 어디 | 왜 급한가 |
|---|---|---|---|
| 1 | **연속 학습일이 실제보다 길게 나올 수 있다** | 미확인 | 9/10 에뮬 확인 중 5일치 기록을 심었는데 화면이 `7일` 이라고 했다. 🔴 **재현도 원인 규명도 안 했다.** 사용자가 자기 기록을 실제보다 좋게 본다. 이번 릴리스와 무관한 기존 자리다 |

### 보안·수익
없음.

### 검증 도구 · 못 본 화면
| 항목 | 왜 |
|---|---|
| 발음 소리를 한 번도 못 들었다 | 플래시카드의 자동 발음. 켜지는 것만 봤고 **소리는 안 났는지 못 났는지 모른다** |
| 만기순 정렬을 화면으로 못 봤다 | 단위 시험만 통과했다. 카드가 실제로 그 순서로 나오는지 안 봤다 |
| 시트 두 개 | 알림 권유 · 복원 확인. 지난주 여백 공통화의 영향을 눈으로 안 봤다 |

### 표면 흠
없음.

### 문서가 코드보다 낡은 것 (되돌리기 사고 방지)
| 문서 | 무엇이 낡았나 |
|---|---|
| `docs/architecture.md` · `screens.md` | **화면 9개**로 적혀 있다. 실제 15개다. 알림 · 간격 복습 · 백업 · 다국어 · 플래시카드가 통째로 없다 |
| `docs/data-model.md` | 저장 키 4개로 적혀 있다. 실제로는 알림 설정 · 언어 · 플래시카드 설정 등이 더 있다 |
| `docs/test-cases.md` | 2026-03-28 에 멈춰 있다(78건). 지금 자동 시험은 284건이다 |
| `README.md` | `화면 13개` · `199개` 시험. 실제 15개 · 284개 |
| `docs/CHANGELOG.md` | 플래시카드가 없다 (위 §0 의 3번) |

🔴 **이 묶음이 위험한 이유**: 다음 사람이 `screens.md` 를 믿고 "이 화면은 없는데?" 로 판단하면 멀쩡한 기능을 지우러 간다.

---

## 2. 9월 10~11일에 끝낸 것

- 스킬 둘이 틀린 규칙을 들고 있던 것 `1ba457f` (창 모드는 `-gpu host` 면 뜬다 · 고지 판정은 가드로)
- `CLAUDE.md` 에 플래시카드 절 `a1bf7ba` (🔴 채점하지 않는 것이 결정이지 미완성이 아니다)
- 게시 중인 오픈소스 고지가 두 릴리스 낡아 있던 것 `0be84b2` (검증: `check:licenses` ALL PASS · 218개)
- 시트 좌우 여백의 주인을 공용 부품으로 옮김 + 뜻 목록 한 곳으로 `333578e` (9곳 정리 · 화면 픽셀 변화 0)
- 카드를 넘기면 카드가 사라지던 것 `097a246` (🔴 타입 검사와 시험 284개를 **전부 통과한 채** 있던 결함이다. 화면으로만 잡혔다)
- 플래시카드 `aa391eb` (홈 3칸 메뉴 · 앞뒤 뒤집기 · 채점 안 함)
- 최적화된 리소스 축소를 켬 `6367fd6` (🔴 **아직 릴리스 빌드로 검증 안 됨** · §0.1)

---

## 3. 대기 중

| 무엇 | 왜 내가 못 정하나 |
|---|---|
| 🔴 **스토어 게시** | 빌드와 검증까지는 내가 하고 **`검토를 위해 전송` 은 사장님이 누른다.** 관리형 게시가 꺼져 있어 통과가 곧 게시이고, 그 뒤로는 되돌릴 창이 사실상 없다 |

✅ 9/11 에 사장님 승인으로 닫힌 것 셋. **월요일에 다시 여쭙지 않는다.**

| 무엇 | 커밋 |
|---|---|
| `CLAUDE.md` 플래시카드 절 | `a1bf7ba` |
| `emulator-test` 스킬의 *"창 모드로 안 뜬다"* 정정 | `1ba457f` |
| `reload-docs` 스킬의 오픈소스 고지 판정법 교체 | `1ba457f` |
