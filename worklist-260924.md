# 수요일(2026-09-24) 작업 목록 — 2026-09-23 세션 인계

> 양식 정본: `C:/project/common/WORKLIST_HANDOVER.md`. 지난 목록: `worklist-260919.md`.
> 🔴 **어제와 성격이 다른 날이었다.** 코드를 한 줄도 안 고쳤고, 새 기능 기획과 결제 인프라 준비를 했다.

---

## 0. 바로 시작할 것: RevenueCat 상품 연결을 끝낸다

- **무슨 일**: 새 기능 `AI 단어 시험` 기획이 확정됐고, 그 수익 모델인 구독(`myword_pro`)의 결제 인프라를 깔던 중이다.
  Play Console 은 **끝났고** RevenueCat 이 **자격증명 대기**로 멈춰 있다.
- 🙋 **사장님 손이 필요한 둘**(9-23 에 못 끝냈다. 세션이 누를 수 없는 자리다)
  1. RevenueCat 앱 설정에 서비스 계정 JSON 올리기
     `https://app.revenuecat.com/projects/3cfc5aec/apps/app8ce5d8bd2e`
     파일 `C:/project/secrets/play-service-account.json` → `Save changes`
  2. Play Console 에서 그 서비스 계정에 **My Word 앱 권한** 주기
     `사용자 및 권한` → `revenuecat-play@optimal-shard-426006-n3...` 행 `관리` → `애플리케이션 추가` → My Word
     체크 셋만: **앱 정보 보기 · 재무 데이터 보기 · 주문 및 구독 관리**
     🔴 `프로덕션으로 출시` 는 켜지 않는다(바로 윗줄이라 스크롤이 흔들리면 잘못 눌린다)
- **확인 순서**
  1. RevenueCat 앱 설정에서 자격증명이 **초록**인가. 노랗거나 `Could not check` 면 ②가 안 된 것이다
  2. ⚠ 재무 권한은 전파가 느리다. **몇 시간 걸릴 수 있고 그동안 다시 하지 않는다**(스킬 §2④)
- **그 뒤 세션이 할 것** (전부 자격증명이 초록이어야 돈다)
  ```
  ① 상품 import        myword_pro:monthly · myword_pro:yearly
  ② entitlement pro 에 두 상품 attach    ← 빠뜨리면 「샀는데 광고가 그대로」가 된다
  ③ offering(current) 에 두 상품 등록     ← 빠뜨리면 앱이 「상품 없음」이라고 한다
  ④ webhook(공통 서버) + RTDN Pub/Sub
  ```
- **정본**: 기획·계획 문서 https://claude.ai/code/artifact/5d6109be-0a60-43a5-8dd7-6c6bc42133d0
  절차는 `~/.claude/skills/store-iap-setup`

---

## 1. 백로그

### 유저 피해가 실재
없음.

### 보안·수익
| 항목 | 왜 | 정본 |
|---|---|---|
| 구독 인프라 미완성 | 위 §0. 이게 끝나야 앱·서버 작업이 의미를 갖는다 | `store-iap-setup` 스킬 |

### 새 기능 「AI 단어 시험」 — 설계는 끝났고 구현이 안 시작됐다
| 항목 | 상태 |
|---|---|
| 기획 | ✅ 확정(아래 §2). 문서 `기획` 탭 |
| 구현 계획 | ✅ Phase 0~8 · 영향 범위 · 데이터 모델 · 리스크 · 테스트. 문서 `구현 계획` 탭 |
| Phase 0 준비 | 🔨 진행 중(위 §0) |
| Phase 1 이후 | ⬜ **서버도 앱도 한 줄 안 짰다** |

### 아직 안 정한 것 (급하지 않다)
1. 성적을 몇 판까지 남기나 — 제 안 20판
2. 단어당 문제 상한 — 제 안 10개. **원가의 천장을 정하는 값이다**
3. 어느 릴리스에 싣나
4. 구독 가격 확정 — 지금 월 4,900 · 연 39,000 은 **임시**다. 원가를 두 달 재고 정한다

### 검증 도구 · 못 본 화면 (9-19 에서 그대로)
| 항목 | 왜 |
|---|---|
| 결과 화면 수정분을 한국어 · 일본어로 못 봤다 | 9-18 확인은 영어뿐 |
| 발음 소리를 한 번도 못 들었다 | 에뮬레이터가 `-no-audio` |
| 만기순 정렬 카드를 화면으로 못 봤다 | |
| 시트 두 개(알림 권유 · 복원 확인) | 여백 공통화 뒤 눈으로 안 봄 |
| ESLint 가 돌지 않는다 | `.eslintrc.js` 가 `@react-native` 설정을 못 찾는다 |

### 표면 흠 (9-19 에서 그대로)
| 항목 | 자리 |
|---|---|
| 플래시카드에서 넘긴 순서가 퀴즈에서 유지되지 않는다 | `QuizScreen` · `docs/screens.md` §7 |
| `틀린 N개 다시 풀기` 의 N 은 오답 건수이고 실제로 내는 것은 단어 수 | `QuizResultScreen` · §8 |
| 쓰이지 않는 것 둘: `onAddWord` prop · `srsService.invalidate` | `screens.md` §2 · `services-api.md` |

### 문서가 코드보다 낡은 것
없음. 가드 3종이 9-23 에 전부 초록이었다.

---

## 2. 끝낸 것

### 9월 23일 (화)

**문서 재적재와 정리** `9643624`
- `/reload-docs` 전수. 가드 3종 초록(`check:docs` · `check:licenses` 218개 · 버전 3중 일치)
- AdMob 광고 제한이 **닫혔다**(사장님 확인 "다 정상"). 넷이 동시에 떨어진 것은 앱 문제가 아니라 크롤링 대기였고 **닷새** 걸렸다
- 공용 문서 넷의 My Word 행을 1.7.0 으로: `PLAY_CONSOLE_STATUS`(3곳) · `PRE_LAUNCH_CHECK` §6 · `R8_OBFUSCATION` §7 · `APP_ADS_TXT` **§1.3 신설**

**새 기능 「AI 단어 시험」 기획 확정** (문서 두 탭)
- 정체: 시험이 아니라 **단어 공급원**이다. 오답 보기가 곧 추천 단어라, 시험을 볼수록 단어장이 자란다
- 로그인 **안 붙인다**(Play 구독으로 판정) · 자격시험이 아닌 **단어·문장 시험** · 객관식 + 문장 빈칸(활용형·해설)
- 점수는 **내 단어 문제만** 통계에 반영하고 **재시험은 반영하지 않는다**(같은 문제를 다시 풀면 외워서 맞힌다)
- 재시험 두 가지(같은 문제 / 새 문제) · 회차별 성적표 · **구독 해지 뒤에도 객관식 재시험까지 된다**(그 채점은 서버를 안 쓴다)
- **문제 창고**: 나누는 단위는 시험이 아니라 **문제 한 개**. 처음 만날 때 축을 나눠 **3개를 한 번에** 만든다
  (사장님 지적으로 바뀐 설계다 — 한 개씩 만들면 두 번째도 같은 문제가 나온다. 그리고 한 번에 만드는 쪽이 총비용도 싸다)
- 서버는 **공통 서버**에 `/api/v1/ai/*` 로. 조각의 `ai-policy.ts`·`admin-pricing.ts` 를 복사해 온다
- 🔴 언어: 문제 본문과 보기는 **한 벌**, 뜻과 해설만 언어별. 안 나누면 창고가 세 배가 된다
- 🔴 **수익 구조를 바꾼다** — 평생 광고제거를 내리고 구독 하나로 묶는다. 상품은 **지우지 않고 비활성화**하고 `ownsRemoveAds` 판정도 남긴다
- 🔴 **앱 정체성 변경을 사장님이 승인했다**("ai 추가하니깐 어쩔수 없지"). `CLAUDE.md`·README·처리방침의 *"서버 없음 · 데이터가 기기 밖으로 안 나감"* 을 구현 단계에서 고친다

**결제 인프라 Phase 0**
- Play Console: 구독 `myword_pro` 생성 · 이름과 설명 **ko/en/ja** · `monthly`(월 4,900원) `yearly`(연 39,000원) · 174개국 · **둘 다 활성**
- RevenueCat: 프로젝트 `My Word`(id `3cfc5aec`) · Android 앱 `com.myword.front`(`app8ce5d8bd2e`) · entitlement **`pro`**
  (공통 서버 `apps.entitlementKeys` 기본값이 `pro` 라 그것에 맞췄다. 서버 설정을 안 건드려도 된다)
- Google Cloud: 할 일이 없었다. 서비스 계정이 이미 있고 이름부터 `revenuecat-play` 다

---

## 3. 대기 중

| 무엇 | 누구를 기다리나 |
|---|---|
| RevenueCat 자격증명 · Play 앱 권한 | 🙋 **사장님**(위 §0). 세션이 누를 수 없는 자리다 |
| 재무 권한 전파 | Google(몇 시간). 노란 상태로 있어도 다시 하지 않는다 |
| 공통 서버 콘솔 「최신 버전(안내)」 값이 1.3.3 | 공통 서버 세션 |

---

## 4. 🔴 브라우저로 콘솔을 만질 때 (오늘 값을 치른 것들)

다음에 같은 헛걸음을 하지 않게 적어 둔다. 전부 2026-09-23 실측이다.

| 증상 | 원인 | 대응 |
|---|---|---|
| 좌표 클릭이 계속 빗나간다 | **뷰포트와 스크린샷 좌표 프레임이 다르다**(2844 vs 1568). 게다가 **작업 중에 뷰포트가 2844 → 2560 으로 바뀌었다** | 누르기 직전에 `getBoundingClientRect()` 로 재고 `프레임폭/innerWidth` 를 곱한다. **기억한 좌표를 재사용하지 않는다** |
| 입력칸에 글자가 안 들어간다 | 페이지 로드 직후 **첫 입력이 씹힌다** | 넣고 **값을 되읽어 확인**한다. 안 들어갔으면 다시 친다 |
| JS 로 버튼을 눌렀는데 아무 일도 없다 | **같은 이름의 버튼이 둘**이었다(하단 바 vs 다이얼로그). `find` 가 엉뚱한 것을 잡았다 | 이름으로 찾은 뒤 **rect 를 찍어 화면 어디인지 본다** |
| 스크린샷·확대가 30초에 끊긴다 | 탭이 **`document.visibilityState === 'hidden'`** 이다 | JS 는 계속 된다. 화면 대신 **JS 로 상태를 읽는다** |
| 프로젝트 이름 칸에 남의 프로젝트 이름이 들어갔다 | 빗나간 클릭이 드롭다운 항목을 집었다 | 🔴 **되돌릴 수 없는 버튼(Create · Save · 활성화) 앞에서는 값을 되읽고 누른다.** 오늘 이것 덕에 잡았다 |

🔴 **그리고 두 가지는 애초에 세션이 못 한다**(안전 장치가 막는다. 판정이 타당하다).

```
자격증명 파일 업로드   결제·환불 권한을 가진 키를 브라우저로 올리는 것
계정 권한 부여         「프로덕션으로 출시」가 바로 옆줄에 있는 그 화면
```

→ **IAP 셋업 절차를 짤 때 이 둘은 처음부터 사람 몫으로 잡는다.** 세션이 거기까지 깔아 두고 넘기는 것이 맞다.
