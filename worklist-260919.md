# 금요일(2026-09-19) 작업 목록 — 2026-09-18 세션 인계

> 양식 정본: `C:/project/common/WORKLIST_HANDOVER.md`. 지난 목록: `worklist-260915.md`(9-14 · 9-18 기록이 거기 있다).

---

## 0. 바로 시작할 것: AdMob 광고 제한이 풀렸나 (다섯 앱 · 2026-09-18 스토어 연결)

- **무슨 일**: AdMob 에서 우리 다섯 앱이 전부 "스토어 미연결 · 광고 게재가 제한됨" 이었다. 9-18 사장님 승인으로 다섯 앱 모두 Play 앱을 연결했다.
  배구명가는 그 자리에서 인증을 통과해 검토 중이다. **My Word · 조각 · LinkMemo · Idea Repository 넷은 인증에서 떨어졌다.**
  광고 확인 파일(`vivace-games.com/app-ads.txt`)은 맞게 있는데, AdMob 이 그 주소를 아직 한 번도 읽어 가지 않았다
- **확인 순서**
  1. AdMob `앱 → app-ads.txt` 탭에 `vivace-games.com` 줄이 생겼나(9-18 에는 배구명가 사이트 한 줄뿐이었다)
  2. `앱` 목록에서 넷이 `검토 필요` → `준비 중 · 검토 중` 으로 넘어갔나. 배구명가는 `준비됨` 이 됐나(검토 2~3일)
  3. 안 넘어갔으면 각 앱의 앱 설정에서 앱 인증을 다시 누른다(`업데이트 확인`)
  4. Play 콘솔 My Word 의 `주의` "app-ads.txt 추가 또는 업데이트" 가 사라졌나
- **파일 확인 한 줄**: `curl -s https://vivace-games.com/app-ads.txt` → `google.com, pub-2731473780180274, DIRECT, f08c47fec0942fa0`
- **정본**: 공용 `APP_ADS_TXT.md` §1.2
- **주의**: AdMob 계정 설정을 바꾸는 동작(앱 인증 다시 누르기는 괜찮다)은 사장님께 여쭌다. 검색창은 페이지를 연 직후 첫 입력이 안 먹는다. 한 번 누르고 다시 친다

---

## 1. 백로그

### 유저 피해가 실재
없음. 9-18 에 찾은 퀴즈 결과 화면 결함 넷(스크롤 · 다시 풀기 · 뒤로가기 · 영어 라벨)은 **고쳐서 OTA 로 냈다**(아래 §2).
사용자 기기는 앱을 다시 열 때 받는다.

### 보안·수익
| 항목 | 왜 | 정본 |
|---|---|---|
| AdMob 광고 제한 | 위 §0. 풀리기 전까지 광고 수입이 제한된다(지난달 US$5.60) | `APP_ADS_TXT.md` §1.2 |

### 검증 도구 · 못 본 화면
| 항목 | 왜 |
|---|---|
| 결과 화면 수정분을 한국어 · 일본어로 못 봤다 | 9-18 확인은 영어 화면뿐. 라벨 폭은 최소 30 을 유지해 한국어는 예전과 같을 것(계산상) |
| 발음 소리를 한 번도 못 들었다 | 에뮬레이터를 `-no-audio` 로 띄운다. 실기기에서 들어 봐야 안다 |
| 만기순 정렬 카드를 화면으로 못 봤다 | 9-14 에 설정 화면의 `10 due` 까지만 봤다 |
| 시트 두 개(알림 권유 · 복원 확인) | 여백 공통화 뒤 눈으로 안 봤다 |
| ESLint 가 돌지 않는다 | `.eslintrc.js` 가 가리키는 `@react-native` 설정을 못 찾는다(9-18 에 봄 · 원인 미조사) |

### 표면 흠
| 항목 | 자리 |
|---|---|
| 플래시카드에서 넘긴 순서가 퀴즈에서 유지되지 않는다(저장소 순서로 낸다) | `QuizScreen` 의 `retryWordIds` 필터 · `docs/screens.md` §7 |
| `틀린 N개 다시 풀기` 의 N 은 오답 건수, 실제로 내는 것은 중복을 걷은 단어 수 | `QuizResultScreen` · `docs/screens.md` §8 |
| 쓰이지 않는 것 둘: 단어장의 `onAddWord` prop · `srsService.invalidate` | `docs/screens.md` §2 · `services-api.md` |

### 문서가 코드보다 낡은 것
없음. 9-18 문서 감사로 설계 문서 넷을 코드에서 다시 뽑았고 `CLAUDE.md` 의 끝난 줄 넷도 닫았다.
재발은 `npm run check:docs` 가 잡는다(화면 · 서비스 · 컴포넌트 · 저장 키).

### 이어서 볼 것 (급하지 않다)
1. Play 콘솔 `권장 조치` 의 R8 항목이 2줄에서 1줄(AGP 9.0 만)로 줄었나
2. 공용 `PLAY_CONSOLE_STATUS.md` My Word 행을 22 (1.7.0) 으로. 🔴 그 파일에 **다른 세션의 커밋 안 된 수정**이 있다. 내 행만 담는다
3. ⏰ 1.7.0 공지 노출이 **9/21 09:00 KST** 에 끝난다. 새 공지를 올릴지는 🙋 사장님 판단
4. 서버 통계를 읽을 때: OTA 는 앱 버전 번호를 바꾸지 않는다. 9-14 · 9-18 OTA 로 버전별 수가 움직여도 사용자 증감이 아니다
5. "연속 학습일이 실제보다 길다"(9-10)는 결함이 아닐 가능성이 크다(단어 등록일도 활동으로 센다). 확정하려면 9-10 의 시드를 다시 대 본다

---

## 2. 끝낸 것

### 9월 18일 (목)
- Play 콘솔 app-ads.txt 경고 원인: My Word · 조각 스토어의 **웹사이트 칸이 비어 있었다.** 두 앱에 `https://vivace-games.com` 을 넣고 게시(사장님 승인) `7e48941`
- 공용 `APP_ADS_TXT.md` 신설 · AdMob 다섯 앱 스토어 연결 기록 (common `c2a1130` · `3eba70b` · `fc7c9c5`) · 인계 `fdb01c9` `bf826c4`
- `/check` 스킬 등록 `2e6b78a` · 사장님이 만든 시트 `my_word` 탭 첫 확인 0건 `4f36bd3`
- 스토어 스크린샷 검토 통과 확인(공개 페이지 한국어 · 영어 새 네 장)
- **문서 감사** `e417bf5`: 설계 문서 넷을 코드에서 다시 뽑았다(9개 화면 → 15개 · 저장 키 4 → 14 · 서비스 4 → 13) · README · test-cases · I18N_PLAN · CHANGELOG 1.5.0 제목
- 가드 `npm run check:docs` 신설(일부러 망가뜨려 빨개지는 것 확인 · 부분 문자열 구멍을 그때 막았다) `e417bf5`
- 스킬: My Word `doc-consistency` 신설 · `reload-docs` 에 가드 추가 `e417bf5` · 공용 `doc-consistency` 를 형제 판 셋과 합쳐 올림 (common `f7b0d5b`)
- 결과 화면 결함을 에뮬레이터로 확인 `ac9f3ae`(증거 `D:/builds/my_word/evidence/2026-09-18/` 1~4번)
- 결과 화면 넷을 고침 `56c6a23`(릴리스 APK · 에뮬레이터 통과 · 증거 5~8번)
- **OTA 발행** `591f63e`(그룹 `428aa669` · 발행 전 번들 주소 검사 통과 · 에뮬레이터 수신 확인)
- `CLAUDE.md` 의 끝난 줄 넷 닫기 + `check:docs` 가리키는 줄(사장님 승인 "고치고 정리해"). 이 목록과 같은 커밋

---

## 3. 대기 중

| 무엇 | 누구를 기다리나 |
|---|---|
| AdMob 이 `vivace-games.com/app-ads.txt` 를 읽어 가는 것 · 네 앱 인증 | Google AdMob(하루쯤). 위 §0 |
| 배구명가 AdMob 앱 검토 | Google AdMob(보통 2~3일) |
| 공통 서버 콘솔 "최신 버전(안내)" 값이 1.3.3 | 공통 서버 세션(알림 보냄). 서버 설정이라 My Word 가 고치지 않는다 |
