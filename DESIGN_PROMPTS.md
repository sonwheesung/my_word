# My Word — UI 전면 리뉴얼 디자인 프롬프트 모음

> 디자인 AI(ChatGPT/Claude)에 그대로 복사해서 던지면 되는 프롬프트 모음입니다.
> **사용 순서:** ① "0. 공통 디자인 시스템" 먼저 → ② 동일 세션에서 화면별 프롬프트 순차 입력.
> 새 세션을 열 경우, 화면 프롬프트 앞에 "0. 공통 디자인 시스템" 결과물을 컨텍스트로 붙여넣을 것.

---

## 0. 공통 디자인 시스템 (가장 먼저 실행)

```
당신은 Apple Human Interface Guidelines와 iOS 26의 Liquid Glass 디자인 언어에
정통한 시니어 프로덕트 디자이너입니다. 모바일 단어 학습 앱 "My Word"의
디자인 시스템을 처음부터 다시 설계해 주세요.

[제품 컨텍스트]
- 이름: My Word
- 형태: React Native(Expo) 기반 안드로이드/iOS 단어 학습 앱 (세로 모드 전용)
- 사용자: 영어 단어를 직접 모으고 퀴즈로 복습하는 한국인 학습자(중고생·대학생·직장인)
- 톤 & 매너: 절제된 프리미엄, 차분한 집중, 가벼운 깊이감 (게이미피케이션 X)
- 데이터: 100% 로컬 저장(백엔드 없음). 사용자 사진/소셜 요소 없음.
- 핵심 가치 키워드: 집중(focus), 정돈(orderly), 진중함(serious), 부드러움(gentle)

[디자인 방향]
- iOS 26 Liquid Glass 스타일을 기본 언어로 채택
  · 반투명 글래스 표면(backdrop-blur 24~40px, 옅은 채도)
  · 깊이감 있는 다층 그림자(soft, layered shadows)
  · 굵은 라운드(16/20/24/28pt), 부드러운 모션
- 다크 모드를 1등 시민으로 설계하고 라이트 모드도 동등하게 지원
- "조용한 고급감" — 채도 낮은 컬러 + 미묘한 그라데이션 + 큰 여백
- 한국어 가독성 최우선 (Pretendard 계열 권장)

[산출물 — 다음 8개 섹션을 모두 포함]
1) 브랜드 무드보드 (3~5개 형용사 + 무드를 설명하는 한 문단)
2) 컬러 시스템
   - Light/Dark 각각: background, surface(elevated), surface-glass(반투명),
     primary, primary-soft, accent, text-primary, text-secondary, text-tertiary,
     border, success, warning, danger
   - 각 컬러는 HEX 또는 rgba로 표기, 사용 예시 한 줄
   - 그라데이션 토큰 2~3종(헤더/카드/포인트)
3) 타이포그래피 스케일
   - Display / Title-1 / Title-2 / Headline / Body / Callout / Caption / Footnote
   - 각 항목: font-size(pt), line-height, weight, letter-spacing, 용도
   - 한글/영문 폰트 패밀리 권장 (예: Pretendard, SF Pro)
4) 스페이싱 & 레이아웃
   - 4pt 그리드, spacing 토큰(2,4,8,12,16,20,24,32,40,56)
   - 라운드 토큰(sm/md/lg/xl/2xl), border 토큰, 화면 안전 영역 규칙
5) Elevation & Glass
   - 그림자 4단계(elev-0 ~ elev-3) 각각 offset/blur/spread/color
   - Glass surface 레시피(blur, bg, border, inner-highlight)
6) 모션 토큰
   - duration(fast/base/slow), easing(spring/standard/emphasized)
   - 화면 전환, 카드 등장, 토스트, 모달, 답안 피드백 각각 추천 모션
7) 핵심 컴포넌트(각각 spec + 라이트/다크 미리보기)
   - Button(primary/secondary/ghost/destructive) + 상태(default/pressed/disabled/loading)
   - Card(default/glass/elevated)
   - Input(text/textarea/inline-edit) + focus/error 상태
   - Chip / Tag / Badge
   - List Item (단어 카드 전용 변형 포함)
   - Tab / Segmented Control
   - Modal / Bottom Sheet
   - Toast / Skeleton / Empty State / Loading State
   - App Header(투명 + 스크롤 시 글래스 모핑)
   - Quiz Choice Card (객관식 보기 전용)
   - Progress Bar(퀴즈 진행도)
   - Streak Heatmap Cell
   - Banner Ad placeholder (AdMob 배너 공간)
8) 접근성 가이드라인
   - 본문 대비비, 터치 타겟 최소 크기, 다이내믹 타입 대응 원칙

[형식 규칙]
- 토큰명은 kebab-case (예: surface-glass, text-primary)
- 컬러는 반드시 HEX 또는 rgba()로 명시
- 각 컴포넌트는 "ASCII 와이어프레임 + 상세 스펙(여백·radius·색·타이포·모션)" 형식
- 결과물 맨 마지막에 "Design Tokens JSON" 한 덩어리로 정리(개발 이식용)
```

---

## 화면별 프롬프트 작성 규칙 (공통)

각 화면 프롬프트는 반드시 다음 8개 블록으로 구성됩니다.

1. **역할** — 시니어 프로덕트 디자이너 역할 부여
2. **이 화면의 목적** — 사용자가 여기서 무엇을 해결하는가
3. **앱 컨텍스트** — 직전 화면, 이후 화면, 진입 경로
4. **정보 구조 & 필수 요소** — 보여야 할 데이터와 액션
5. **실데이터 모델** — 실제 필드명/타입
6. **상태(state)** — 빈/로딩/에러/성공 케이스
7. **인터랙션 & 모션** — 탭/스와이프/입력/피드백 흐름
8. **산출 요청** — ASCII 와이어프레임 + 컴포넌트 spec + 토큰 사용 + 카피라이팅(한국어)

---

## 1. HomeScreen — 메인 대시보드

```
당신은 iOS 26 Liquid Glass 언어에 정통한 시니어 프로덕트 디자이너입니다.
앞서 정의한 "My Word" 디자인 시스템을 사용하여 HomeScreen을 디자인해 주세요.

[목적]
사용자가 앱을 열자마자 "오늘 무엇을 할지" 결정하도록 돕는 허브 화면.
공부 동기를 부드럽게 자극하면서 핵심 액션 2개로 즉시 진입.

[컨텍스트]
- 앱 진입 시 가장 먼저 보이는 화면
- 우측 상단 설정 버튼 외 별도 헤더 없음
- 하단에 AdMob 배너(약 50pt 높이) 공간 필수

[정보 구조 — 위에서부터]
1) Hero 영역 (글래스 + 부드러운 그라데이션)
   - 인사말("반갑습니다!") + 앱 타이틀("My Word")
   - 우측 상단: 설정(톱니) 아이콘 버튼
   - 통계 카드(가로 3분할): 등록 단어 / 정답률 / 연속 학습일
   - Streak 메시지 배너(연속 학습일에 따라 동적 문구)
2) Primary 액션 그리드 (2열)
   - "학습하기" (icon: school) — 퀴즈로 단어 복습
   - "단어 추가" (icon: add) — 새로운 단어 등록
3) Secondary 메뉴 그리드 (4열 작은 카드)
   - 단어장(menu-book) / 카테고리(folder-open) / 통계(bar-chart) / 마이(person)
4) 하단 광고 배너 영역

[실데이터]
- summary: {
    totalWords: number,
    totalCategories: number,
    accuracy: number,          // 0~100
    totalQuizCount: number,
    streakDays: number
  }
- 미학습 상태(totalWords=0 && totalQuizCount=0)에서는 통계 대신
  Empty Hero("단어를 추가하고 학습을 시작해보세요!") 노출

[상태]
- 로딩: 통계 영역에 ActivityIndicator 또는 스켈레톤
- 빈 상태(미학습): 통계 카드 → Empty Hero CTA로 치환
- 일반: 통계 + Streak 배너 동시 노출

[인터랙션]
- 화면 진입 시 fade + slide-up (600ms, emphasized easing)
- 카드 탭 시 스케일 0.97 + 햅틱 light
- 설정 아이콘 탭 → Settings 화면 push 전환

[카피라이팅 규칙]
- streakDays === 0: "오늘 첫 학습을 시작해보세요!"
- 1~2: "{n}일 연속 학습 중! 계속 가보자!"
- 3~6: "{n}일 연속! 좋은 습관이 만들어지고 있어요"
- 7+: "{n}일 연속! 대단해요!"

[산출 요청]
- 라이트 모드 + 다크 모드 ASCII 와이어프레임 각 1
- 미학습 상태 와이어프레임 1
- Hero 글래스 스펙(blur/bg/그라데이션/그림자) 명시
- Primary/Secondary 카드의 정확한 토큰 사용 표기
- 진입 애니메이션 시퀀스 다이어그램(시간선)
```

---

## 2. ManageWordsScreen — 단어장(단어 목록 관리)

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
ManageWordsScreen을 디자인해 주세요.

[목적]
저장된 모든 단어를 카테고리별로 탐색·검색·편집·삭제할 수 있는
"내 단어장"의 메인 관리 화면.

[컨텍스트]
- Home에서 "단어장" 진입 또는 다른 화면에서 회귀
- 상단 카테고리 탭으로 필터, 우측에 카테고리 관리 진입
- 검색은 단어/뜻/태그를 동시에 매칭(클라이언트 사이드, 300ms debounce)

[정보 구조]
1) 상단 App Header (글래스, 스크롤 시 활성)
   - 좌측: 뒤로가기
   - 중앙: "단어장"
   - 우측: 더보기(점 3개) → 카테고리 관리, CSV 가져오기, 정렬
2) 카테고리 가로 스크롤 칩 (선택된 칩은 강조)
   - "전체" + 사용자 카테고리들 + "+ 새 카테고리"
3) 검색 바 (글래스 인풋, 좌측 search 아이콘, 우측 clear)
4) 카운트/정렬 줄 ("234개 · 최근 추가순 ▾")
5) 단어 목록 (긴 스크롤)
   - 단어 카드(elevated): 단어(영문, Title-2) / 뜻 미리보기(최대 2개) /
     태그 칩(최대 3개, 초과 시 +N) / 우측 chevron
   - 좌측 스와이프 → 편집(파란색), 우측 스와이프 → 삭제(빨간색, 확인 모달)
   - 다중 선택 모드 진입: 카드 long-press
6) FAB(우측 하단): + 단어 추가 (Primary 그라데이션 + 글래스 후광)

[다중 선택 모드]
- 활성화 시 헤더가 "{n}개 선택"으로 모핑
- 하단에 액션 바 등장(슬라이드 업): 카테고리 이동 / 삭제 / 취소

[실데이터]
- Word: { wordId, categoryId, word, meanings[], examples[], tags[], memo, createdAt, updatedAt }
- Category 칩에서 선택 시 categoryId === null이면 전체

[상태]
- 빈 카테고리: 일러스트 + "이 카테고리에 단어가 없어요" + "단어 추가" 버튼
- 검색 결과 0건: "'{검색어}'에 해당하는 단어가 없어요"
- 첫 사용: 카테고리 자체가 0개 → "첫 카테고리를 만들어보세요" + CTA 2개(카테고리 / 단어 추가)
- 로딩: 단어 카드 스켈레톤 5~6개

[인터랙션]
- 카테고리 칩 탭 시 부드러운 가로 스크롤 + 칩 강조 모션
- 카드 탭 → 편집(AddWordScreen, editMode=true)
- 카드 스와이프: spring easing, 임계점 통과 시 햅틱 medium
- 삭제 확인 모달: 빨간색 강조, "취소" 보다 "삭제" 가 약하게

[산출 요청]
- 라이트/다크 와이어프레임 각 1
- 다중 선택 모드 와이어프레임 1
- 빈 상태(3종) 각각의 와이어프레임
- 카드 스와이프 시퀀스(4컷)
- FAB의 글래스 효과 상세 스펙
```

---

## 3. AddWordScreen — 단어 추가 / 편집

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
AddWordScreen을 디자인해 주세요. 이 화면은 신규 추가와 편집 모드를
동일 UI로 처리합니다.

[목적]
사용자가 한 단어를 등록(또는 수정)하기 위한 입력 화면.
영어 단어인 경우 외부 사전 API로 한국어 뜻/예문/품사를 자동 채워주고,
사용자가 원하는 만큼 뜻·예문·태그·메모를 자유롭게 추가.

[컨텍스트]
- 진입 경로: Home, ManageWords, CategoryManage 등
- 저장 성공 시 이전 화면으로 자동 복귀 + 토스트 노출
- 키보드와의 공존이 매우 중요 (ScrollView + keyboardShouldPersistTaps="handled")

[정보 구조 — 위에서부터]
1) Header
   - 좌측: 뒤로(미저장 시 확인)
   - 중앙: "단어 추가" 또는 "단어 편집"
   - 우측: "저장" 텍스트 버튼 (입력 유효 시 활성)
2) 카테고리 선택 셀렉터 (글래스 카드, 탭 시 바텀시트 등장)
3) "단어" 입력 (Text-Field, Title-1 굵게)
   - 영어로 판별되면 우측에 "사전 자동 채움" 아이콘 버튼 등장
   - 자동 채움 진행 중: 인풋 우측에 작은 spinner + "찾는 중..."
   - 성공 시 토스트("사전에서 뜻 5개, 예문 3개를 가져왔어요")
   - 실패 시 인라인 안내 "사전을 찾지 못했어요. 직접 입력해주세요"
4) "뜻" 섹션 (여러 개 가능)
   - 각 행: 뜻 입력 + 우측 trash 아이콘
   - 맨 아래: "+ 뜻 추가" 점선 카드
   - 최소 1개 필수, 최대 10개 제한
5) "품사" Chip 그룹 (자동 채움 시 표시, 토글 가능)
6) "예문" 섹션 (여러 개 가능, 접기/펼치기 가능)
   - 각 카드: 예문(영어/원문) 입력 + 번역(선택) 입력 + 삭제
   - "+ 예문 추가"
7) "태그" 섹션
   - 입력 후 엔터/쉼표로 태그 칩 생성, 칩 클릭으로 제거
8) "메모" Textarea (자유 입력)
9) 하단 sticky 영역: "저장" 버튼(primary, full-width)

[실데이터]
- WordRequest: {
    categoryId: number,
    word: string,
    meanings: string[],
    examples: { example: string, translation?: string }[],
    tags?: string[],
    memo?: string
  }
- 영어 판별: /^[a-zA-Z\s'-]+$/ 매치 시
- 중복 검사: 같은 categoryId 내 word.toLowerCase() 일치 → 경고 모달

[키보드 정책]
- "단어" 입력: autoCapitalize="none", autoCorrect=false, returnKeyType="next"
- "뜻" 입력: keyboardType="default", 한국어 키보드 우선, returnKeyType="next"
- "메모"만 multiline=true
- 모든 입력은 trim, 입력별 maxLength + 남은 글자수 카운터
- 폼 입력 간 ref 기반 포커스 이동

[상태]
- 신규 모드: 모든 필드 비어있음, 저장 비활성(필수만 채워지면 활성)
- 편집 모드: 기존 값 프리필, 변경된 경우만 "저장" 강조 펄스
- 사전 lookup: 인라인 로딩(단어 인풋 우측)
- 저장 중: 저장 버튼 disabled + spinner
- 저장 성공: 짧은 success 햅틱 + 토스트
- 중복 발견: 모달("이미 등록된 단어입니다. 어떻게 할까요?")

[인터랙션]
- 섹션별 카드 등장 stagger 애니메이션
- "+ 추가" 버튼 탭 시 새 행이 spring으로 펼쳐짐
- 삭제 시 행 collapse + fade
- 키보드 등장 시 입력 필드가 키보드 위로 부드럽게 스크롤

[산출 요청]
- 빈 신규 / 자동채움 완료 / 편집 모드 와이어프레임 각 1
- "사전 자동 채움" 진행 중/성공/실패 3컷 시퀀스
- 카테고리 선택 바텀시트 와이어프레임
- 키보드 등장 상태 와이어프레임
```

---

## 4. CategoryManageScreen — 카테고리 관리

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
CategoryManageScreen을 디자인해 주세요.

[목적]
단어를 묶는 카테고리(폴더)를 추가·이름변경·정렬·삭제하기 위한 화면.

[컨텍스트]
- 진입: Home/ManageWords의 메뉴 또는 카테고리 칩 끝 "+"
- 카테고리 삭제는 안의 단어도 함께 삭제(돌이킬 수 없는 작업)

[정보 구조]
1) Header: 뒤로 + "카테고리 관리" + "+" 추가
2) 카테고리 카드 리스트 (드래그 정렬 가능)
   - 각 카드: 햄버거 핸들(좌) + 카테고리 이름 + 단어 수 배지 + 더보기(우)
   - 더보기 → 이름 변경 / 삭제
3) 빈 상태: "첫 카테고리를 만들어보세요" + CTA
4) 추가/편집: 인라인 또는 바텀시트 입력 (이름, 설명 optional)

[실데이터]
- Category: { categoryId, categoryName, description?, displayOrder, wordCount, createdAt, updatedAt }
- 정렬 변경 시 displayOrder를 일괄 업데이트

[상태]
- 드래그 중: 카드 elev-3로 부상 + 다른 카드 살짝 흐려짐
- 삭제 확인: 모달 + 단어 수 강조("이 카테고리의 단어 32개도 함께 삭제됩니다")
- 빈 상태, 로딩 스켈레톤

[인터랙션]
- long-press → 드래그 모드, spring scale 1.02
- 드롭 시 햅틱 medium + 위치 확정 애니메이션
- 이름 변경: 인라인 텍스트 → tap to edit 패턴

[산출 요청]
- 일반/드래그 중/빈 상태 와이어프레임 각 1
- 카드 더보기 메뉴 와이어프레임 1
- 삭제 확인 모달 와이어프레임 1
```

---

## 5. QuizSetupScreen — 퀴즈 설정

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
QuizSetupScreen을 디자인해 주세요.

[목적]
사용자가 어떤 단어를, 어떻게 풀지를 단 한 화면에서 빠르게 결정.

[컨텍스트]
- Home의 "학습하기"에서 진입
- 모든 설정이 결정된 후 "시작" 탭 → QuizScreen으로 전환

[정보 구조 — 위에서부터 세로 폼]
1) Header: 뒤로 + "퀴즈 설정"
2) Section1 — 카테고리
   - 가로 스크롤 칩: "전체" + 사용자 카테고리
   - 선택 시 칩 강조(글래스 highlight + outline)
3) Section2 — 출제 모드 (Segmented 또는 큰 카드 3개)
   - 랜덤: 무작위로 섞어서
   - 순서: 등록한 순서대로
   - 취약: 정답률 낮은 단어 위주(50% 미만)
   - 각 카드에 짧은 부제 + 아이콘
4) Section3 — 출제 수 (Chip 그룹)
   - 10 / 20 / 30 / 50 / 전체
5) Section4 — 출제 방향 (Segmented Control 2칸)
   - "단어 → 뜻"  |  "뜻 → 단어"
6) Section5 — 답안 유형 (Segmented Control 2칸)
   - "주관식 입력"  |  "객관식 4지선다"
7) Footer sticky: "시작" Primary 버튼 + 보조 텍스트("선택된 단어 20개")

[실데이터]
- 입력 결정값: categoryId(null=전체), mode('random'|'sequential'|'weak'),
  wordCount(number|'all'), direction('word_to_meaning'|'meaning_to_word'),
  answerType('subjective'|'multiple_choice')
- "선택된 단어 수" 카운트는 실시간 계산 결과(필터 + 모드 적용 후)

[상태]
- 단어 없음: 카드("아직 학습할 단어가 없어요") + "단어 추가" CTA
- 취약 단어 0개인데 모드=weak: 안내 "취약 단어가 충분하지 않아요. 다른 모드를 추천해요"
- 객관식 가능 단어 < 4개: 안내 "보기를 만들기엔 단어가 부족해요"

[인터랙션]
- 옵션 선택 시 즉시 하단 카운트 업데이트(숫자 모핑)
- 시작 버튼은 모든 조건이 충족된 순간 약한 펄스 1회
- 시작 탭 → 화면 전체가 위로 슬라이드되며 QuizScreen이 fade-in

[산출 요청]
- 라이트/다크 와이어프레임 각 1
- 단어 부족 상태 와이어프레임 1
- 시작 버튼 sticky 처리 디테일(글래스 푸터)
```

---

## 6. QuizScreen — 퀴즈 진행

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
QuizScreen을 디자인해 주세요.

[목적]
한 문제씩 풀며 즉시 정/오답 피드백을 받고 다음 문제로 자연스럽게 이어지는
"몰입 학습" 화면. 화면 자체가 학습의 공간이 되어야 함.

[컨텍스트]
- 진입: QuizSetupScreen → "시작"
- 종료 조건: 마지막 문제 답안 제출 → QuizResultScreen 자동 전환
- 중도 종료: 좌상단 X → 확인 모달("진행 상황이 사라져요")

[정보 구조]
1) 미니 Header
   - 좌: X (종료)
   - 중앙: 진행도 카운터 "3 / 10"
   - 우: 카테고리 미니 칩
2) Progress Bar (얇은 글래스 트랙 + Primary fill, spring으로 채워짐)
3) 문제 카드 (화면 중앙 큰 글래스 카드)
   - direction에 따라 "단어" 또는 "뜻" 표시
   - direction='word_to_meaning': 단어를 크게(Display)
   - direction='meaning_to_word': 뜻을 Title-1로
   - 보조: 품사 칩, 카테고리 이름 (작게)
4) 답안 영역 (answerType 분기)
   - subjective: 큰 입력 인풋 + "확인" 버튼
   - multiple_choice: 4개 선택 카드(세로 스택), 카드 탭 = 즉시 제출
5) 피드백 오버레이 (답안 제출 후)
   - 정답: 초록 글래스 시트가 아래에서 슬라이드 업
     · 큰 체크 + "정답!" + 정답어 + 예문(있으면 1개)
   - 오답: 빨간 글래스 시트
     · X 아이콘 + "오답" + 내가 쓴 답 + 정답 + 예문
   - 하단: "다음" 버튼 (또는 자동 2.5초 후 진행 옵션)

[실데이터]
- 문제: Word에서 추출 (word/meanings/examples/categoryId)
- 객관식 보기: 같은 categoryId 풀에서 정답 외 3개 무작위 추출,
  단어 풀이 부족할 경우 전체 단어에서 보완
- 결과 누적: QuizResult[]

[상태]
- 입력 비어있을 때: "확인" 비활성
- 객관식 보기 로딩(첫 진입): 카드 스켈레톤
- 정답 입력 후 확인 누르기 전까지 키보드 유지(키보드 위에 확인 버튼 떠 있음)

[인터랙션]
- 문제 전환: 우→좌 슬라이드 + 새 카드 fade-in, 200ms
- 정답: 화면 전체에 옅은 초록 글로우 1회 + 햅틱 success
- 오답: 카드 횡 진동(shake) + 햅틱 warning
- 객관식 카드 탭 즉시: 선택 카드 outline + 다른 카드 흐려짐 → 피드백
- 진행도 바: 답안 제출 직후 spring으로 다음 칸 채움

[산출 요청]
- 주관식 진행 / 주관식 정답 피드백 / 주관식 오답 피드백 각 1
- 객관식 진행 / 객관식 정답 피드백 / 객관식 오답 피드백 각 1
- 중도 종료 모달 와이어프레임
- 모션 시퀀스 다이어그램(문제 진입 → 입력 → 제출 → 피드백 → 다음)
```

---

## 7. QuizResultScreen — 퀴즈 결과

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
QuizResultScreen을 디자인해 주세요.

[목적]
이번 회차 성적을 한눈에 보여주고, 다음 학습 동선("다시", "틀린 것만",
"홈으로")으로 자연스럽게 이어주는 마무리 화면.

[정보 구조]
1) Hero (큰 글래스 + 그라데이션)
   - 정답률 도넛 차트 (애니메이션으로 0%부터 차오름)
   - 중앙: "8 / 10" 큰 타이포 + "정답률 80%"
   - 짧은 격려 메시지 (정답률에 따라 동적)
2) 빠른 요약 카드
   - 정답 수 / 오답 수 / 소요 단어 수 / 모드 / 방향
3) 결과 리스트
   - 각 행: 단어 / 내 답 / 정답 / O 또는 X 배지
   - 오답은 살짝 강조(좌측 라인)
4) Footer sticky (3 버튼)
   - "틀린 것만 다시" (오답 있을 때만 활성)
   - "다시 풀기" (같은 설정으로)
   - "홈으로"

[격려 메시지 규칙]
- 100%: "완벽해요!"
- 80~99%: "훌륭한 결과예요!"
- 60~79%: "잘하고 있어요"
- 40~59%: "한 번 더 복습해볼까요?"
- 0~39%: "괜찮아요, 반복이 답이에요"

[실데이터]
- props: { correctCount, totalCount, results: QuizResult[] }
- 오답 wordIds 집계 → "틀린 것만 다시"의 retryWordIds로 전달

[상태]
- 결과 0건(예외 케이스): 안내 + "홈으로"만 노출
- 도넛 차트는 화면 진입 후 600ms에 spring으로 시작

[인터랙션]
- 진입 시 도넛 + 숫자 카운트업 동시 모션
- 결과 행은 stagger 50ms 간격 fade-in
- 버튼 탭 시 햅틱 light

[산출 요청]
- 100% / 80% / 40% 케이스 각 1
- 도넛 차트 spec(반지름/두께/색/모션)
- 오답 없는 경우 푸터 와이어프레임 1
```

---

## 8. StatisticsScreen — 통계

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
StatisticsScreen을 디자인해 주세요.

[목적]
사용자가 본인의 학습 흐름을 데이터로 객관화 — 무엇을 잘하고 무엇을
못하는지 시각적으로 빠르게 파악.

[정보 구조]
1) Header: 뒤로 + "통계"
2) 요약 카드 (가로 2x2 또는 세로 스택)
   - 총 풀이 수 / 정답률 / 등록 단어 / 취약 단어
3) 카테고리별 정답률 (수평 바 차트 카드)
   - 각 카테고리: 이름 + 단어수 + 정답률 + 컬러 바 + 취약 N개
   - 탭 시 해당 카테고리의 단어별 통계로 드릴다운
4) 단어별 정답률 (탭 또는 필터로 진입)
   - 정렬: 정답률 낮은 순(기본) / 풀이수 많은 순 / 가나다 순
   - 각 행: 단어 + 작은 막대 + 정답률% + 풀이수 + 정/오 카운트
5) 빈 상태: "아직 통계가 없어요. 퀴즈를 풀어보세요!"

[실데이터]
- QuizStatistics: { totalQuizCount, correctCount, incorrectCount, accuracy,
                    totalWordCount, totalCategoryCount, weakWordCount }
- CategoryQuizStats[] (drilldown용)
- WordQuizStats[] (filterCategoryId optional)

[상태]
- 데이터 없음: 빈 상태 일러스트 + CTA
- 카테고리 0개: 안내 + 카테고리 만들기 CTA
- 로딩: 카드별 스켈레톤

[인터랙션]
- 카테고리 카드 탭 → 바텀시트로 단어별 리스트 펼침(또는 push)
- 정렬 토글: 칩 그룹
- 막대 차트는 진입 시 좌→우로 spring 채움

[산출 요청]
- 라이트/다크 와이어프레임 각 1
- 빈 상태 / 카테고리 드릴다운 바텀시트 와이어프레임
- 막대 차트 시각화 토큰(색/높이/간격) 명시
```

---

## 9. MyPageScreen — 마이페이지(활동·연속학습)

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
MyPageScreen을 디자인해 주세요.

[목적]
"내가 얼마나 꾸준히 했는가"를 시각적으로 자랑할 수 있는 페이지.
GitHub 잔디 같은 활동 히트맵과 streak이 핵심.

[정보 구조]
1) Header: 뒤로 + "마이"
2) Streak Hero (글래스 + 부드러운 그라데이션)
   - 불꽃 아이콘 + "{n}일 연속" 큰 타이포
   - 보조: "총 활동일 {m}일"
   - streak === 0: "오늘 시작해볼까요?" 메시지
3) 누적 카드 (가로 2분할)
   - 총 등록 단어 / 총 풀이 수
4) 활동 히트맵 (잔디)
   - 최근 12주 또는 6개월
   - 셀당 활동 강도 4단계 색조(빈 셀 포함)
   - 셀 hover/tap: 날짜 + 단어수 + 퀴즈수 툴팁
   - 좌측 요일 라벨, 상단 월 라벨
5) 최근 활동 타임라인 (선택)
   - 날짜별 "단어 N개 추가, 퀴즈 M개 풀이"

[실데이터]
- MyPageStats: { totalWordCount, totalQuizCount, streakDays, totalActiveDays,
                 activities: { date, wordCount, quizCount }[] }

[상태]
- 활동 없음: Streak Hero는 격려 카피, 히트맵은 회색 그리드 + 안내
- 데이터 적음(7일 미만): 히트맵 대신 "오늘부터 기록이 쌓여요" 카드

[인터랙션]
- Streak 숫자 카운트업
- 히트맵 셀 stagger fade-in (좌→우, 위→아래)
- 셀 탭 → 작은 글래스 툴팁(상단 위치, 화살표)

[산출 요청]
- 활동 많음 / 활동 적음 / 활동 0 각 1
- 히트맵 셀 컬러 스케일(다크/라이트 각각 4단계 + 빈 셀)
- Streak Hero 글래스 + 그라데이션 상세 스펙
```

---

## 10. ImportWordsScreen — CSV 가져오기

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
ImportWordsScreen을 디자인해 주세요.

[목적]
사용자가 외부에서 만든 단어 목록을 CSV 형식으로 붙여넣어 한 번에
대량 등록하도록 돕는 화면. 데이터 안전성과 미리보기가 핵심.

[정보 구조]
1) Header: 뒤로 + "단어 가져오기"
2) 안내 카드 (접기/펼치기)
   - 포맷 설명: 단어,뜻,예문,태그,메모
   - 구분자 규칙: 뜻/예문/태그는 "|"로 구분, 예문은 "예문::번역"
   - "예시 보기" 토글 → 샘플 3줄
3) 카테고리 선택 셀렉터
4) CSV 입력 영역 (큰 textarea, monospace, 줄번호 표시)
   - 우측 상단: 붙여넣기 버튼, 지우기 버튼
   - 하단 메타: "{n}줄"
5) "파싱하기" 버튼 (full-width)
6) 파싱 결과(파싱 후 등장)
   - 통계 칩: 새 단어 N / 중복 M / 오류 K
   - 탭 3개 (새 단어 / 중복 / 오류)
     · 새 단어 리스트(체크박스로 선별 가능)
     · 중복 리스트(어떤 단어와 중복인지 표시)
     · 오류 리스트(줄번호 + 사유 + 원문)
7) Footer sticky: "{n}개 가져오기" 버튼

[실데이터]
- ParseResult: { success: ParsedWord[], errors: { line, content, reason }[] }
- DuplicateCheckResult: { newWords, duplicateWords }
- 각 ParsedWord: { word, meanings[], examples[], tags[], memo }

[상태]
- 빈 입력: 파싱 버튼 비활성, "예시 붙여넣기" 보조 버튼 노출
- 파싱 중: 버튼 spinner
- 결과 0건: 안내 + 형식 다시 보기
- 가져오기 진행: 진행도 바 + 카운터

[인터랙션]
- 결과 탭 전환 시 부드러운 슬라이드
- 오류 카드 탭 → 해당 줄을 입력 영역에서 하이라이트
- 가져오기 성공 시 토스트 + ManageWords로 복귀

[산출 요청]
- 빈 입력 / 파싱 후 결과 / 오류 케이스 와이어프레임 각 1
- 안내 카드(접힘/펼침) 와이어프레임
- 결과 탭 컴포넌트 디테일 spec
```

---

## 11. SettingsScreen — 설정

```
당신은 시니어 프로덕트 디자이너입니다. "My Word" 디자인 시스템으로
SettingsScreen을 디자인해 주세요.

[목적]
앱 환경 설정·테마 변경·데이터 관리·법적 정보 접근 등 부가 기능을
정돈된 리스트 형태로 제공.

[정보 구조 — 그룹 리스트(iOS 스타일)]
1) Header: 뒤로 + "설정"
2) 그룹 A — 테마
   - 현재 테마 미리보기 카드 (작은 컬러 칩 + 이름)
   - 탭 시 바텀시트로 테마 갤러리(라이트/다크/시즌별 등)
3) 그룹 B — 학습 환경
   - 퀴즈 기본 설정(출제 수/모드) — 기본값 저장 토글
   - 효과음 / 햅틱 토글 (있다면)
4) 그룹 C — 데이터
   - 단어 내보내기(CSV 공유)
   - 단어 가져오기(ImportWords 진입)
   - 전체 데이터 초기화 (빨강, 2단계 확인)
5) 그룹 D — 정보
   - 앱 버전 (탭 시 업데이트 확인)
   - 개인정보처리방침
   - 오픈소스 라이선스 (있다면)
   - 문의/피드백
6) Footer 영역
   - 작은 로고 + "Made with care"

[실데이터]
- 테마: ThemeContext (themeId, setThemeId)
- 버전: APP_VERSION vs LATEST_VERSION
- 데이터 초기화 시 clearAllData() (모든 AsyncStorage 키 삭제)

[상태]
- 업데이트 있음: 버전 행에 작은 배지 + "새 버전 있음"
- 초기화 확인: 2단계 모달
  1차: "정말 모든 단어와 기록을 삭제할까요?"
  2차: "이 작업은 되돌릴 수 없어요. 계속하시려면 '삭제'를 입력하세요" (입력 검증)

[인터랙션]
- 행 탭 시 chevron이 살짝 우측으로 밀리는 마이크로 인터랙션
- 테마 변경 시 화면 전체 색이 부드럽게 크로스페이드(300ms)
- 토글: spring 모션, 햅틱 light

[산출 요청]
- 라이트/다크 와이어프레임 각 1
- 테마 갤러리 바텀시트 와이어프레임
- 데이터 초기화 2단계 모달 와이어프레임
- 리스트 그룹 헤더/구분선 스타일 디테일
```

---

## 사용 팁

1. **반드시 "0. 공통 디자인 시스템"부터 실행**해서 컬러/타이포/스페이싱 토큰을 먼저 받으세요. 이후 화면 프롬프트가 토큰명을 가정합니다.
2. 새 채팅 세션을 열 때는 "0번 결과물"을 컨텍스트로 먼저 붙여넣으세요.
3. 디자인 AI가 한 번에 처리하는 양이 부담스러우면, 각 화면 프롬프트의 "[정보 구조]" 섹션만 분리해서 단계적으로 요청해도 됩니다.
4. 만족스러운 결과가 나오면 결과물을 그대로 저장해서, 후속 화면 요청 시 "지금까지 정의한 컴포넌트와 일관되게 만들어 주세요"라고 짧게 덧붙이면 일관성이 유지됩니다.
5. RN(Expo) 구현 단계에서 토큰 JSON을 그대로 `src/constants/themes.ts`와 컴포넌트 토큰으로 이식하세요.
